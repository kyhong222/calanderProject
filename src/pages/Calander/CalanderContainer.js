import React from 'react';
import CalanderPresenter from './CalanderPresenter';

function CalanderContainer() {
  // const { loaded, username, userData, interestedTag } = useUserState();
  return (
    (
      <>
        <CalanderPresenter/>
      </>
    )
  );
}

export default CalanderContainer;
